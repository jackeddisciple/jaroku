"""Generated agent projects live here, one package per agent: agents/<agent_id>/.

Written by the Jaroku builder (server/src/generator.ts), never by hand. Each subpackage is
standard LangGraph the user owns — no Jaroku imports. See jaroku_runner for how they run.
"""
