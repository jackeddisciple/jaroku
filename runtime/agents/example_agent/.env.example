# This agent needs no credentials — its tools are dependency-free.
#
# To run it against a real provider, set the relevant key in runtime/.env
# (not here) and pick the provider in the Jaroku UI:
#
#   ANTHROPIC_API_KEY=
#   OPENAI_API_KEY=
